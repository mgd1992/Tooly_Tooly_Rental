<div class="container mt-5">
  <h1><%= @category.name %> tools:</h1>

  <ul>
    <% @tools.each do |tool| %>
      <li><%= link_to tool.name, tool_path(tool) %></li>
    <% end %>
  </ul>

  <%= link_to "new tool", tool_path(tool) %>
</div>