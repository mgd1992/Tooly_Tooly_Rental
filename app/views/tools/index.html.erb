<div class="container mt-5">
  <h1>All tools</h1>

  <ul>
    <% @tools.each do |tool| %>
      <li><%= link_to tool.name, tool_path(tool) %></li>
    <% end %>
  </ul>

  <%= link_to "Add a new tool", new_list_path %>
</div>
