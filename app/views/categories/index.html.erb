<h1>Categories:</h1> 

<% @categories.each do |category| %>

    <div class="container d-flex justify-content-center border p-5 w-50">
    <%= link_to category.name, categories_path(category) %>
    </div>

<% end %>
